import type { StylusCompileConfig } from './types.js';

/**
 * Default configuration for Stylus compilation.
 */
export const DEFAULT_STYLUS_COMPILE_CONFIG: StylusCompileConfig = {
  useHostToolchain: false,
};
