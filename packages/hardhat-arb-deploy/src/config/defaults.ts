import type { StylusDeployConfig } from './types.js';

/**
 * Default configuration for Stylus deployment.
 */
export const DEFAULT_STYLUS_DEPLOY_CONFIG: StylusDeployConfig = {
  useHostToolchain: false,
};
